# PansouAndPanCheck 环境变量配置示例

# pansou 服务地址
SEARCH_API_URL=http://127.0.0.1:8888

# PanCheck 服务校验接口地址
CHECK_API_URL=http://127.0.0.1/api/v1/links/check

# 应用端口（默认1566）
PORT=1566

# 应用主机地址（默认0.0.0.0）
HOST=0.0.0.0

# HTTP客户端超时时间（秒，默认60.0）
CLIENT_TIMEOUT=60.0

# 日志级别（DEBUG, INFO, WARNING, ERROR, CRITICAL，默认INFO）
LOG_LEVEL=INFO

# 是否启用认证（true/false，默认false）
AUTH_ENABLED=false

# 登录账号，格式：user1:pass1,user2:pass2
AUTH_USERS=admin:admin123

# JWT有效期（小时，默认24）
AUTH_TOKEN_EXPIRY=24

# JWT签名密钥；生产环境建议显式配置固定强随机字符串
AUTH_JWT_SECRET=please-change-me

# 上游 pansou 是否启用认证（true/false，默认false）
PANSOU_AUTH_ENABLED=false

# 上游 pansou 登录账号；启用 PANSOU_AUTH_ENABLED 且未配置 PANSOU_AUTH_TOKEN 时必填
PANSOU_AUTH_USERNAME=WebAdmin

# 上游 pansou 登录密码；启用 PANSOU_AUTH_ENABLED 且未配置 PANSOU_AUTH_TOKEN 时必填
PANSOU_AUTH_PASSWORD=PansouWeb

# 可选：固定上游 pansou token；配置后优先使用，不再自动登录
PANSOU_AUTH_TOKEN=

# 是否开启调试模式（true/false，默认false）
DEBUG=false
